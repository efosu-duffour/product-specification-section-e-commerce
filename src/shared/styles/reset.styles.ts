import { css } from "lit";

export const resetStyles = css`
 :host {
   * {
    padding: 0;
    margin: 0;
    box-sizing: border-box;
   }

   :host {
    font-family: inherit;
    font-size: inherit;
    color: inherit;
   }
 }
`